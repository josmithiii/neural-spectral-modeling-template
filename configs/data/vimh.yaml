_target_: src.data.vimh_datamodule.VIMHDataModule

# Bass onset synthetic dataset generated by generate_vimh.py
data_dir: ${paths.root_dir}/data/vimh-32x32x1_44100Hz_1p0s_2048dss_bass_onset_3p

# Same loader settings as your original vimh.yaml
batch_size: 128           # adjust if you want
num_workers: 0            # keep 0 for MPS / Mac compatibility
pin_memory: False
persistent_workers: False
prefetch_factor: 4
cache_dataset: true
