# @package _global_

# Tiny CNN on small (256 samples) trivial dataset
# python src/train.py experiment=trivial_tiny_small

defaults:
  - override /data: vimh
  - override /model: cnn_tiny
  - override /callbacks: default
  - override /trainer: default

tags: ["trivial", "tiny", "small_data"]

seed: 12345

trainer:
  min_epochs: 3
  max_epochs: 8

model:
  optimizer:
    lr: 0.003
    weight_decay: 0.001

data:
  data_dir: data/vimh-32x32x1_8000Hz_1p0s_256dss_simple_2p
  batch_size: 32

logger:
  tensorboard:
    name: "trivial_tiny_small"
