# @package _global_

# Wah Pedal CNN experiment: Quantized Regression Loss
# python src/train.py experiment=wah_cnn_tiny_quantized

defaults:
  - override /data: vimh
  - override /model: cnn_tiny_quantized # Lightweight quantized regression loss
  - override /callbacks: default
  - override /trainer: default

tags: ["wah", "cnn_tiny", "quantized_regression"]

seed: 12345

# Use the correct metric for regression models (MAE instead of accuracy)
optimized_metric: "val/mae_best"

trainer:
  min_epochs: 20
  max_epochs: 200

callbacks:
  model_checkpoint:
    dirpath: ${paths.output_dir}/checkpoints
    filename: "epoch_{epoch:03d}"
    monitor: "val/mae_best"
    mode: "min"
    save_last: True
    auto_insert_metric_name: False
  early_stopping:
    monitor: "val/mae_best"
    patience: 100
    mode: "min"

model:
  optimizer:
    lr: 0.01
    weight_decay: 0.01
  scheduler:
    _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
    _partial_: true
    mode: min
    factor: 0.5
    patience: 3

data:
  data_dir: data/vimh-32x32x1_8000Hz_1p0s_16384dss_saw_wah_2p  # Use larger dataset (16k samples)
  batch_size: 64

logger:
  tensorboard:
    name: "wah_cnn_tiny_quantized"